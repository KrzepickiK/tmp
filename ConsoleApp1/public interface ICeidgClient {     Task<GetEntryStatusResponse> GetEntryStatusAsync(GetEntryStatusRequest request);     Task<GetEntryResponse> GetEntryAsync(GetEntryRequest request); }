public static class MappingExtensions
{
    public static List<TTarget> MapList<TSource, TTarget>(
        this IEnumerable<TSource> source,
        Func<TSource, TTarget> map)
    {
        return source?.Select(map).ToList() ?? new List<TTarget>();
    }
}
